apiVersion: v1
kind: Service
metadata:
  name: wishlist-service
  labels:
    app: wishlist-service
spec:
  ports:
  - name: "8081"
    port: 8081
    targetPort: 8081
  type: NodePort
  selector:
    app: wishlist-service
