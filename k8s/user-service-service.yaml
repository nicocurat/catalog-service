apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  ports:
  - name: "9001"
    port: 9001
    targetPort: 9001
  selector:
    app: user-service
status:
  loadBalancer: {}
